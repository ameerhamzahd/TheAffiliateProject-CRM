'use client';

import React from 'react';

const Logo = () => {
    return (
        <div>
            <p className='text-[color:var(--color-high)]'>logo</p>
        </div>
    );
};

export default Logo;